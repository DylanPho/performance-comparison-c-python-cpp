#include <stdio.h>

// A simple C function to print a message
void print_message() {
    printf("Hello from the C function!\n");
}

// A simple C function to add two integers and return the result
int add_numbers(int a, int b) {
    return a + b;
}
